// Connector registrations. Providers land one task at a time in Phase 1.
export function registerBuiltinConnectors(): void {
  // shopify (1.2), meta (1.3), google_ads (1.4) register here as they land.
}
